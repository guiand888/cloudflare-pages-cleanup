[project]
name = "cloudflare-pages-cleanup"
version = "0.1.0"
description = "Scheduled Cloudflare Worker that prunes old Cloudflare Pages deployments across an account."
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]
